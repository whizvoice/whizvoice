repos:
  - repo: local
    hooks:
      - id: python-tests
        name: run python tests
        entry: python -m unittest discover tests
        language: system
        pass_filenames: false
        types: [python]
